# Type stub for the componentize-py generated bindings of the `kernel`
# WIT world (runtime/wit/kernel.wit). Exists for the editor/LSP only —
# at build time componentize-py provides the real module inside the
# guest. Keep in sync with kernel.wit.

def host_effect(name: str, payload: str) -> str: ...
